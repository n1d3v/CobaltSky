<UserControl x:Class="CobaltSky.UserControls.PostEmbed"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Root"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignWidth="480"
    Width="Auto" HorizontalAlignment="Stretch">

    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneBackgroundBrush}" HorizontalAlignment="Stretch">
        <Border BorderBrush="Gray" BorderThickness="1" Padding="8,12">
            <StackPanel>
                <Image x:Name="EmbedImage" Source="{Binding Thumb, ElementName=Root}" Width="325" HorizontalAlignment="Center" />
                <StackPanel Margin="12,8">
                    <TextBlock x:Name="EmbedTitle" Text="{Binding Title, ElementName=Root}"  TextWrapping="Wrap" FontWeight="SemiBold" />
                    <TextBlock x:Name="EmbedDescription" Text="{Binding Description, ElementName=Root}"  TextWrapping="Wrap" Opacity="0.75" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>