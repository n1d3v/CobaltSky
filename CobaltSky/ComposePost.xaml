<phone:PhoneApplicationPage
    x:Class="CobaltSky.ComposePost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="12,0,0,12">
            <TextBlock Text="compose" Margin="9,-7,0,6" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>
        
        <Grid x:Name="ContentPanel" VerticalAlignment="Top" Grid.Row="1" Margin="26,0,26,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                <Image Source="Images\user-pfp.png" Width="70" VerticalAlignment="Top"></Image>
                <StackPanel>
                    <TextBox BorderThickness="2" Background="#ffffff" Width="370" TextWrapping="Wrap" HorizontalAlignment="Stretch" Margin="0,-12,-12,0" Padding="15,14,15,14" VerticalAlignment="Top" />
                    <TextBlock Text="0 / 300" Opacity="0.5" TextAlignment="Right" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar>
            <shell:ApplicationBarIconButton IconUri="Images\AppBar\upload.png" Text="post" />
            <shell:ApplicationBarIconButton IconUri="Images\AppBar\pictures.png" Text="albums" />
            <shell:ApplicationBarIconButton IconUri="Images\AppBar\videos.png" Text="videos" />
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>
</phone:PhoneApplicationPage>